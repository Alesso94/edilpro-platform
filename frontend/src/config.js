// Configurazione dell'applicazione
const config = {
  // URL del backend
  API_URL: 'http://localhost:3002',
  
  // Altre configurazioni
  APP_NAME: 'Piattaforma Edilizia',
  VERSION: '1.0.0'
};

export default config; 